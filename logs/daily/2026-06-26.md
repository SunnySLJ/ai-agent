# 2026-06-26 Daily Log

## 今天目标

- 岗位能力：AI Agent/RAG 工程化部署、Java 业务工具集成、Qdrant 向量检索。
- 对应资料：`../../agent/part12-agent-docker`、`../../agent/part10-agent`、`../../agent/part13-agent-score`。
- 最小交付：完成 Docker Compose 真启动冒烟，并记录 Python Agent 调 Java 工具服务和 Qdrant 向量库的证据。

## 今天学了什么

- Python Agent/RAG 主链路可以通过 `JAVA_TOOL_BASE_URL` 在容器网络里调用 Java 工具服务。
- 面试作品集不能只证明 `docker compose config`，还要证明容器真实启动、health 通过、业务链路可用。
- OpenAI-compatible LLM adapter 要保持可选，默认仍要支持无 key 的离线确定性演示。
- Qdrant 可以作为 Agent/RAG 项目的生产化检索证据：collection、points upsert、query、payload citation 都要能解释。

## 今天做了什么

- 启动 Docker Desktop，并运行 `docker compose -f compose.yaml up --build -d`。
- 首次拉取 Maven、Temurin、Python 基础镜像并构建 `agent-platform` 和 `java-business-tool-service`。
- 验证 `docker compose -f compose.yaml ps` 中两个服务均为 `healthy`。
- 验证 `http://127.0.0.1:8000/health` 和 `http://127.0.0.1:8080/health` 均返回 `{"status":"ok"}`。
- 调用 Python `/ask`：`查询订单 ORD-1001 的状态`，返回 successful `get_order_status` trace，证明 Python 容器已调用 Java 订单工具。
- 新增 `HashingEmbeddingModel`、`QdrantVectorIndex` 和 `QdrantRetriever`，支持 deterministic embedding、chunk upsert、vector query 和 citation。
- 新增 `QDRANT_BASE_URL`、`QDRANT_COLLECTION` env wiring，FastAPI 可切换到 Qdrant retriever。
- 更新 `compose.yaml`，加入 `qdrant/qdrant` 服务，并让 Python Agent 容器接入 `http://qdrant:6333`。
- 发现 Qdrant 可能返回 0 或负分结果后，补充回归测试并过滤非正分，避免无关 citation 污染业务工具回答。
- Docker build 中 `pip install` 曾因 `files.pythonhosted.org` read timeout 失败，已去掉非必需 `uvicorn[standard]` extras，并给 Dockerfile 增加 `--timeout 120 --retries 10`。
- 固化最终技术栈决策：项目不是纯 Python，也不是 Python + TypeScript 替代 Java；Python 做 Agent/RAG 主链路，Java 做企业业务工具层。
- 新增离线混合检索路线：BM25-style 词法检索 + local hashing-vector 候选 + lightweight rerank，并补 retrieval eval hit rate / MRR 报告。
- 建立每日 AI 行业资讯收集要求：后续每天写 `logs/industry/YYYY-MM-DD.md`，把资讯转成学习、作品集或求职动作。
- 尝试真实 OpenAI-compatible smoke，默认 endpoint 返回 401 invalid_api_key；同时补了 LLM 错误详情脱敏回归，避免供应商错误里带出 key 片段。
- 完成 AI 行业资讯自动采集 MVP：新增来源配置、标准库 RSS/Atom 采集脚本、GitHub Actions 每日 09:00 北京时间定时任务，并生成 `logs/industry/2026-06-26.md`。
- 使用 OpenAI-compatible 远端 gateway 跑通真实模型 smoke：`OpenAICompatibleChatClient` 成功返回真实模型答案，FastAPI `/ask` 路径也成功返回答案和 1 条 citation。
- 提交 `7cd1577 feat: automate ai industry watch`，但推送到 GitHub 时被拒绝：当前 GitHub token 缺少 `workflow` scope，不能创建 `.github/workflows/industry-watch.yml`。
- 尝试推进 BOSS 登录态岗位复核：Chrome 当前未运行，默认 profile 的 Codex Chrome Extension 已安装启用；继续前需要用户同意启动 Chrome。
- 补齐 BOSS 登录态筛岗记录格式：新增 `docs/templates/boss-screening-log.md` 和 `logs/applications/README.md`，后续 20 个岗位按统一字段落表。
- 复核当前测试证据并修正文档：Agent Platform 当前为 29 tests，根目录当前为 12 tests，完成度审计已同步本地未推送提交列表。
- 新增最终完成门禁：`scripts/completion_gate.py` 会检查未推送提交、GitHub workflow scope 和 BOSS 20 岗位复核；当前输出 `Complete: no`，明确剩余 3 个 blocker。
- 优化完成门禁：脚本现在动态输出 `Unpushed Commits`，完成度审计不再手写本地提交列表，避免每次新增提交后文档立刻过期。
- 优化完成门禁下一步：脚本现在输出 `Next Actions`，包括补 GitHub `workflow` scope、推送 main、同意启动 Chrome 后记录 20 个 BOSS 登录态岗位。

## 可写进简历或面试的内容

- 作品集支持 Docker Compose 一键启动 Python Agent API、Java Business Tool Service 与 Qdrant。
- Python Agent 通过环境变量接入 Java 工具服务，工具调用 trace 会记录 tool name、arguments、result、success 和 latency。
- 项目具备离线确定性演示、Java 业务工具集成、OpenAI-compatible LLM 接口、Qdrant 向量检索、评估数据集和 Docker 运行时。
- 离线检索已从单一关键词升级为混合检索和轻量重排，可以讲清楚 keyword、dense、rerank、hit rate、MRR 的工程取舍。

## 还讲不清楚的问题

- 默认 `OPENAI_API_KEY` 仍会返回 401 invalid_api_key；可用的真实 smoke 路径是把 `OPENAI_BASE_URL` 指向 OpenAI-compatible gateway，并配置有效 bearer token 与模型。
- 当前向量检索使用 deterministic hashing embedding，还没有真实 embedding provider 或外部 rerank model。
- BOSS 登录态岗位还没有形成 20 个岗位的真实筛选记录。
- 最新本地提交还没有推到 GitHub，需要给 GitHub token 增加 `workflow` scope。
- AI 行业资讯自动采集已完成脚本和首次日志证据，但还缺连续日志和每周趋势复盘。
- AI 行业资讯自动采集已能跑；本次 LlamaIndex、Qdrant 来源出现 SSL 失败，已写入 `logs/industry/2026-06-26.md` 的“待复核”，后续要优化重试和来源稳定性。
- 默认 OpenAI endpoint 的 `OPENAI_API_KEY` 仍无效；远端 OpenAI-compatible gateway 已完成成功 smoke。

## 明天最小下一步

- 给 GitHub CLI token 增加 `workflow` scope 后，执行 `git push origin main`。
- 同意启动 Chrome 后，登录 BOSS 并筛 20 个杭州 AI Agent/RAG/大模型应用岗位，写入 `logs/daily/`。
- 跟进 `logs/industry/2026-06-26.md` 里的 MCP Python SDK、LangChain provider release 和 OpenAI agents 工作场景文章，复核工具契约与面试表达。
