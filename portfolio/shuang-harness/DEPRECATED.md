# DEPRECATED

本目录已废弃。Harness 工程已迁移至仓库根目录：

```text
../../harness-agent/
```

请使用：

```bash
cd ../../harness-agent
python3 -m venv .venv && .venv/bin/pip install -e .
hstack init "我的新产品想法"
```

文档见 [harness-agent/README.md](../../harness-agent/README.md)。
