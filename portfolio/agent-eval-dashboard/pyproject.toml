[project]
name = "agent-eval-dashboard"
version = "0.1.0"
description = "Evaluation report runner for the AI Agent portfolio"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src", "../agent-platform/src"]
testpaths = ["tests"]
