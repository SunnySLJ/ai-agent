# 会话交接 · 005-openapi-mcp-tool-contract

## 上次做到哪

已创建 feature 文档，边界锁定为 OpenAPI 3.0.3 合约、MCP `tools/list` 风格 manifest、API handoff 文档和合同测试。不实现完整 MCP runtime。

## 下次会话要做的事

1. 读 `state.md`，当前任务是 T02。
2. 先写失败的合同测试，确认缺少 `openapi.json` / `mcp-tools.json`。
3. 按 TDD 执行 T02 到 T08。

## 禁止重新规划

plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。

