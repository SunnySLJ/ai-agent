# 会话交接 · 004-agent-java-tool-integration

## 上次做到哪

已根据用户最新确认恢复 Java + Python 结合方向。004 feature 边界锁定为 Python Agent Platform 通过 HTTP 调 Java Business Tool Service，同时保留 offline deterministic tools。

## 下次会话要做的事

1. 读 `state.md`，当前任务是 T02。
2. 先写失败的 Python 测试，确认缺少 `agent_platform.java_tools` 或 `AgentPlatform.with_java_tools`。
3. 按 TDD 执行 T02 到 T09。

## 禁止重新规划

plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。

