# 会话交接 · 009-eval-dataset-expansion

## 上次做到哪

已完成评估数据集扩展：当前 20 条 eval case 覆盖引用回答、工具调用和拒答三类行为，deterministic offline eval 全部通过。

## 下次会话要做的事

1. 若继续补作品集硬实力，优先接真实 OpenAI-compatible API 或向量库。
2. 若继续补求职闭环，优先做 BOSS 登录态岗位筛选日志。

## 禁止重新规划

009 已收口。本 feature 不接真实模型、不接向量库、不改 Java 服务。
